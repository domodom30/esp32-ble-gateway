; PlatformIO Project Configuration File

[env]
platform = espressif32
framework = arduino
monitor_speed = 921600
upload_speed = 921600

lib_deps = 
    bblanchon/ArduinoJson@^7.0.0
    links2004/WebSockets@^2.3.3
    h2zero/NimBLE-Arduino@^2.2.0
    esp32_https_server@^1.0.0

monitor_filters = esp32_exception_decoder
board_build.partitions = partitions_custom.csv

;[env:esp-wrover-debug]
;board = esp32dev
;build_type = debug
;build_flags =
;    -DCORE_DEBUG_LEVEL=3
;    -DLOG_LOCAL_LEVEL=ESP_LOG_INFO

    ; -DBOARD_HAS_PSRAM
    ; -mfix-esp32-psram-cache-issue

[env:esp-wrover]
board = esp32dev
build_flags =
    ; -DBOARD_HAS_PSRAM
    ; -mfix-esp32-psram-cache-issue
    ; -DCORE_DEBUG_LEVEL=5
    ; Taille de pile loop fixée via SET_LOOP_TASK_STACK_SIZE() dans src/main.cpp.
    ; -DCONFIG_ARDUINO_LOOP_STACK_SIZE est inopérant (redéfini par sdkconfig.h).